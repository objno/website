import styled from 'styled-components';
import {
  flexbox,
  space,
  layout,
  typography,
  border,
  color
} from 'styled-system';

const Box = styled.div`
  ${flexbox}
  ${space}
  ${layout}
  ${typography}
  ${border}
  ${color}
`;

export default Box;
